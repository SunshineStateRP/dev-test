const multiplyByTwo = (num: number) => num * 2;

const divideByThree = (num: number) => num / 3;

const sliceString = (str: string) => str.slice(0, 5);

export { multiplyByTwo, divideByThree, sliceString };